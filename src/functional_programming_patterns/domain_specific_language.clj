(ns functional-programming-patterns.domain-specific-language)

; examples using it as a script language i.e. shell commands

; clojure not good for scripting, startup too slow, so didn't bother
; doing exercises
